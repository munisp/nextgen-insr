# DATA INTEGRITY & OPERATIONS audit — munisp/nextgen-insr @3d32fbd

Non-aspirational: only gaps proven by code/config evidence. "Fixability" = effort class.

| # | Scenario | Evidence | Severity | Fixability | Confidence |
|---|----------|----------|----------|------------|------------|
| 1 | "WORM" audit bucket is not actually WORM — object lock never enabled | `infra/minio/init-minio.sh:44` creates `insureportal-audit-logs` with plain `mc mb` (no `--with-lock`); lines 58-62 admit "Object lock must be enabled at bucket creation time. Re-create with lock if needed" — never done; no `mc retention set` anywhere (grep: 0 hits for retention/ObjectLock/COMPLIANCE/GOVERNANCE). Ransomware-guard claim aspirational; audit bucket mutable/deletable. | High | Medium (recreate bucket with lock + retention, migration of objects) | High |
| 2 | Migration rollback path unrehearsed & unsafe: deploys use `drizzle-kit push --force` which auto-accepts data-loss statements; `db-migrate-safe.sh` (backup-gated migrate) wired to nothing | `MIGRATION_ROLLBACK.md` §1 (self-verified): `Makefile.production:133` `pnpm db:push`; `.github/workflows/integration.yml` push `--force`; `scripts/db-migrate-safe.sh` calls `drizzle-kit migrate` but no workflow invokes it. Drizzle emits no down-migrations. Journal integrity broken: `drizzle/meta/_journal.json` has 41 entries for 46 sql files, skips 0005/0013, and `drizzle/0000_conscious_guardian.sql` is orphaned (not in journal; two `0000_*` files exist). | High | Medium | High |
| 3 | Pre-migration backup written unencrypted to world-readable /tmp | `scripts/db-migrate-safe.sh:22` `BACKUP_DIR="${BACKUP_DIR:-/tmp/insureportal-backups}"`; `pg_dump | gzip > $BACKUP_FILE` (no GPG, default umask). Full DB incl. BVN/NIN sits plaintext on shared /tmp. | High | Low (trivial: default to secure dir + encrypt) | High |
| 4 | Backup encryption optional, off by default | `scripts/backup/pg_backup.sh:10,143-155`: GPG encryption only `if BACKUP_ENCRYPTION_KEY` set; no validation/fail-closed in prod. Plaintext dumps to disk/S3 otherwise. (Restore rehearsal exists — `scripts/backup-restore-rehearsal.sh` + `.github/workflows/backup-rehearsal.yml` — so "restore never tested" is NOT a finding.) | Medium | Low | High |
| 5 | PII (BVN, NIN, names, phones, DOB) stored plaintext at rest; field-level encryption subsystem exists but is a separate metadata table, not applied to customer/agent PII columns | `drizzle/schema.ts:7,9` header lists `customers: name,email,phone,address,date_of_birth,bvn,nin`; `schema.ts:1089-1090,1410-1411` `bvn: varchar(11), nin: varchar(11)` plaintext columns. `encrypted_fields` table (`schema.ts:3758`, migration `0052`) is a generic key/value store used only by `server/routers/encryptedFieldsCrud.ts`; grep shows no insert path encrypting customer/agent PII. `server/lib/piiScanner.ts:417` itself recommends "Implement field-level encryption for PII" — unimplemented. | High | High | High |
| 6 | GDPR/NDPR erasure vs immutable audit chain: erasure anonymizes customer rows but PII persists in append-only hash-chained audit_log (incl. erasure request itself with agent code/reason; other actions store agent names), with no anonymization path and WORM-offloading an admitted OPEN ITEM | `server/routers/gdprDashboard.ts:241-326` anonymizes `customers`+`transactions.customerPhone` only; `gdpr.ts:210-230` writes `GDPR_ERASURE_REQUEST` audit rows containing `agentCode`, reason, `requesterEmail` retained in `data_rights_requests`; `server/lib/auditChain.ts:28` "WORM store … — OPEN ITEM"; `server/routers/auditCompliance.ts:176` admits chain can be rewritten by superuser and WORM is "next step". Deleting/editing audit rows breaks chain verification (`auditChain.ts:123`), so erasure of audit-held PII is structurally impossible today — conflict acknowledged in strings but unresolved in mechanism. | Medium | High | High |
| 7 | Kafka consumer (legacy `kafkaConsume`) silently drops failed messages — no retry, no DLQ; kafkajs auto-commits offset when eachMessage resolves | `server/kafka.ts:154-166`: `catch (err) { logger.warn(...) }` then resolves; no `autoCommit: false`, no DLQ publish, no retry counter. Handler failure = message lost with only a warn log. | High (if used) | Low | High for code, Medium for live exposure (no in-repo caller of `kafkaConsume(` found — appears dormant/legacy) |
| 8 | Poison-pill / DLQ envelope incompatibility between producer and consumer + retryCount reset → infinite redelivery cycle | Producer side `server/kafka-event-consumer.ts:525-549` `sendToDLQ` forwards **raw original message value** with metadata only in headers (`x-original-topic`, hardcoded `"x-retry-count": maxRetries`). Consumer side `server/kafka-dlq-consumer.ts:59-75` `parseMessage` JSON-parses the **body** as `DlqPayload` (expects `originalTopic`, `retryCount` in body; ignores headers) → original events lack these fields → `retryMessage` sends to `topic: undefined` (throws) and `payload.retryCount` is undefined so `< MAX_RETRIES` is always true; re-injected `_retryCount` in body is never read back (`sendToDLQ` re-wraps raw value) → poison message loops DLQ→topic→DLQ forever. | High | Medium (unify envelope schema) | High |
| 9 | Fluvio↔Kafka bridge drops messages on produce failure and its consumer goroutine dies permanently after one reconnect failure | `fluvio-integration/fluvio-kafka-bridge.go:212-235`: `reader.ReadMessage` (kafka-go auto-commits offset) then `Produce` failure only `log.Printf` — message lost. Lines 247-265: channel close → single reconnect attempt; on failure `return` — goroutine exits, bridge direction dead silently, no restart supervisor, no metric. | Medium | Low | High |
| 10 | Settlement/business-date logic uses UTC dates while business timezone is Africa/Lagos (UTC+1): date boundary mis-attributes settlements/audit between 23:00–00:00 Lagos; cron times don't encode Lagos timezone | `server/settlementCron.ts:161,168,200` `new Date().toISOString().split("T")[0]` (UTC) for settlement date/resourceId; cron `"0 16 * * 1-5"` (16:00 server TZ, unspecified `node-cron` timezone option) at `settlementCron.ts:412-445`; workflowId `settlement-${date}` from UTC date (`server/temporal.ts:92,172`); yet settlement schedule config says `cutoffTime "22:00", timezone "Africa/Lagos"` (`seed.mjs:1048`). Same UTC-slice pattern in `insurance-journeys-ext.ts:885`, `journey-activities-extended.ts:521`. | Medium | Medium | High |
| 11 | Temporal workflows have no versioning (`patch`/`deprecatePatch`, worker build-ID) — any workflow-code change breaks or nondeterministically replays in-flight workflows; worker shutdown relies solely on SDK defaults | grep of `server/temporal-workflows.ts`, `server/temporal.ts`, `server/temporal-worker.ts`, journey workflow files: 0 hits for `patch(`, `patched`, `buildId`, `workerVersioning`. `temporal-worker.ts:87-104` plain `Worker.create` + `run()` (no buildId option). | Medium | Medium | High |
| 12 | No JWT/session key rotation support — single symmetric secret, no `kid`/dual-key window; rotation invalidates every session; CSRF secret silently degenerates to per-process random when JWT_SECRET unset | `server/_core/sdk.ts:190-220` `SignJWT ... HS256` / `jwtVerify(cookieValue, secretKey)` with single `getSessionSecret()`, no `kid`, no previous-key list; `server/_core/keycloak.ts:111` single JWKS but app-level sessions not covered; `server/lib/securityAuditFixes.ts:85` `CSRF_SECRET = process.env.JWT_SECRET ?? secureRandomString(64)` — per-process random CSRF key breaks all CSRF tokens on restart and across replicas. | Medium | Medium | High |
| 13 | `/api/ready` gates traffic on only 3 deps (postgres, redis, tigerbeetle) — Kafka, MinIO, Temporal, Keycloak outages leave readiness green | `server/_core/index.ts:754-790`: checks `postgres`, `redis`, `tigerbeetle` only. `/api/health` (line 649) probes minio/kafka/keycloak/tbSidecar but only `db === "connected"` affects status (`allCritical`). Settlement (Temporal), eventing (Kafka), docs (MinIO) can be down while k8s routes traffic. | Medium | Low | High |
| 14 | Two competing SIGTERM handlers registered on the same process with mismatched timeouts and duplicated module state → shutdown race (one path can `process.exit` before the other finishes draining) | `server/_core/index.ts:56,132` imports/calls `setupGracefulShutdown(server)` (registers SIGTERM/SIGINT in `server/lib/gracefulShutdown.ts:80`, force-exit 10s, closes drizzle db/redis/kafka, then `process.exit(0)` immediately without waiting for `server.close` callback) AND index.ts defines its own `gracefulShutdown` (lines 938-1001, force-exit 30s, drains pool again). Both fire on SIGTERM; gracefulShutdown.ts's `process.exit(0)` (line ~72, right after scheduling, not inside close callback) can kill the process before in-flight HTTP drains. Also `server/lib/highAvailability.ts:395` and `server/middleware/index.ts:272` register further SIGTERM handlers. | Medium | Low | High |
| 15 | Session JWT verification sets no explicit clock-skew tolerance; expiry math uses server-local `Date.now()` both for sign and verify — any skew vs Keycloak-issued tokens/other verifiers unmitigated (jose default `clockTolerance` = 0) | `server/_core/sdk.ts:213` `jwtVerify(cookieValue, secretKey, { algorithms: ["HS256"] })` — no `clockTolerance`; `server/lib/realtimeNotifications.ts:166` same pattern; `server/_core/keycloak.ts:111` no tolerance option. Contrast: webhook path handles skew via `Math.abs(now-timestamp) > TOLERANCE` (`server/lib/webhookSignature.ts:43-48`) but `server/middleware/webhookHmac.ts:71-87` verifies HMAC with **no timestamp at all** (replay window unlimited; skew-immune by omission). | Low-Medium | Low | High |
| 16 | MinIO init uses default root credentials minioadmin/minioadmin | `infra/minio/init-minio.sh:17-18` `MINIO_ACCESS_KEY:-minioadmin` / `SECRET:-minioadmin` defaults; bootstrap script will happily run against a default-cred server. | Medium | Low | High |

## Non-findings (checked, adequately handled)
- Restore rehearsal: `scripts/backup-restore-rehearsal.sh` + `.github/workflows/backup-rehearsal.yml` (dump→drop→restore with row-count assertions) — restore IS drilled.
- Seed idempotency: `seed.mjs:59`, `seed-integration.mjs:38`, `scripts/seed-production-final.mjs:52` — deterministic ids + `ON CONFLICT DO NOTHING`.
- Webhook retry DLQ: `server/lib/sprint23Features.ts:575-602`, `webhookRetry.ts:63` — maxAttempts + dead_letter states exist.
- Event-consumer exactly-once dedupe: `server/kafka-event-consumer.ts` processed_events UNIQUE marker in single transaction — redelivery-safe.
- Graceful shutdown (single-path) incl. health flip and 30s drain exists — the defect is duplication (#14), not absence.
- Webhook signature timestamp tolerance exists in `webhookSignature.ts` (but see #15 for the other middleware).
