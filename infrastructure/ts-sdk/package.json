{
  "name": "@ngapp/infra-sdk",
  "version": "1.0.0",
  "description": "NGApp Infrastructure SDK — unified clients for all 12 platform components",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "ioredis": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
