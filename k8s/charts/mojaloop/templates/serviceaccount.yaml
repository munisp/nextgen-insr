apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mojaloop.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mojaloop.labels" . | nindent 4 }}
automountServiceAccountToken: false
