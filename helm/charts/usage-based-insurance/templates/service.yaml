apiVersion: v1
kind: Service
metadata:
  name: {{ include "usage-based-insurance.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "usage-based-insurance.name" . }}
