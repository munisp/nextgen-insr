apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-360-view.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "customer-360-view.name" . }}
