apiVersion: v1
kind: Service
metadata:
  name: {{ include "nigerian-bank-integrations.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "nigerian-bank-integrations.name" . }}
