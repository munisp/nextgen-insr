apiVersion: v1
kind: Service
metadata:
  name: {{ include "disaster-recovery-module.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "disaster-recovery-module.name" . }}
