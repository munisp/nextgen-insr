apiVersion: v1
kind: Service
metadata:
  name: {{ include "mobile-money-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "mobile-money-service.name" . }}
