apiVersion: v1
kind: Service
metadata:
  name: {{ include "enhanced-kyc-kyb.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "enhanced-kyc-kyb.name" . }}
