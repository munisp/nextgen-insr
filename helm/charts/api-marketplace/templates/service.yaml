apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-marketplace.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "api-marketplace.name" . }}
