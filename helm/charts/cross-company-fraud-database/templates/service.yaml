apiVersion: v1
kind: Service
metadata:
  name: {{ include "cross-company-fraud-database.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "cross-company-fraud-database.name" . }}
