apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamification-service.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "gamification-service.name" . }}
