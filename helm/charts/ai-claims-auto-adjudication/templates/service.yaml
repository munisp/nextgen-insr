apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-claims-auto-adjudication.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
  selector:
    app: {{ include "ai-claims-auto-adjudication.name" . }}
