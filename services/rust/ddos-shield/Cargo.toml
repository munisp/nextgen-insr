[package]
name = "ddos-shield"
version = "0.1.0"
edition = "2021"
description = "Adaptive DDoS mitigation / rate-limiting enforcement sidecar for the InsurePortal API"

[dependencies]
actix-web = "4"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
log = "0.4"
env_logger = "0.10"
